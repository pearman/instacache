{
  "name": "instacache",
  "version": "0.0.3",
  "description": "A simple async caching library powered by rxjs.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && jest",
    "format": "prettier --single-quote --print-width 120 --write \"src/**/*.ts\"",
    "watch": "tsc --watch"
  },
  "author": "pearman",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.5",
    "rxjs": "^5.5.6"
  },
  "devDependencies": {
    "jest": "^22.4.2",
    "prettier": "^1.10.2",
    "typescript": "^2.7.2"
  }
}
